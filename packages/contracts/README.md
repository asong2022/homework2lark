# API Contracts

`openapi.json` is generated from FastAPI and is the public contract source for the Web application.

```powershell
npm run contracts
```

Do not edit `openapi.json` or `apps/web/src/lib/generated-api.ts` by hand.
